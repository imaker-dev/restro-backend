name: restro_installer_reference
description: Reference/template files for the Restro POS installer. Not compiled directly.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path: ^1.9.0
